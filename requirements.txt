pandas>=2.0
openpyxl>=3.1          # reading .xlsx
mysql-connector-python>=8.2
matplotlib>=3.8

# Optional — only needed for the model-written commentary (step 13).
# Everything else runs without it.
anthropic>=0.40

# Development
pytest>=7.4
